<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %> - WhatsApp Bot Admin</title>

  <!-- Fuente + Tailwind -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: { sans: ['Inter', 'ui-sans-serif', 'system-ui'] },
          colors: {
            primary: { DEFAULT: '#667eea', 600: '#667eea', 700:'#5a67d8' },
            whatsapp: { DEFAULT: '#25D366', dark: '#128C7E' },
          },
          boxShadow: {
            soft: '0 4px 6px -1px rgba(0,0,0,.08)',
            card: '0 10px 30px -5px rgba(0,0,0,.1)',
          }
        }
      },
      darkMode: 'media'
    }
  </script>

  <!-- Icons -->
  <script src="https://unpkg.com/lucide@latest"></script>

  <style>
    @media (prefers-reduced-motion: reduce){
      *{
        animation-duration: 0.001ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.001ms !important;
        scroll-behavior: auto !important;
      }
    }

    .sidebar-link-active {
      background: linear-gradient(135deg, rgba(255,255,255,.15) 0%, rgba(255,255,255,.08) 100%);
      color: #fff !important;
      font-weight: 600;
      box-shadow: 0 2px 8px rgba(0,0,0,.1);
    }

    /* Chat layout */
    .chat-container {
      display: grid;
      grid-template-columns: 350px 1fr;
      height: calc(100vh - 180px);
      gap: 1rem;
    }

    @media (max-width: 768px) {
      .chat-container {
        grid-template-columns: 1fr;
        height: calc(100vh - 140px);
      }
      .contacts-panel.hidden-mobile {
        display: none;
      }
    }

    /* Custom scrollbar */
    .chat-messages::-webkit-scrollbar {
      width: 6px;
    }
    .chat-messages::-webkit-scrollbar-track {
      background: transparent;
    }
    .chat-messages::-webkit-scrollbar-thumb {
      background: rgba(148, 163, 184, 0.3);
      border-radius: 3px;
    }

    /* Message bubbles */
    .message-bubble {
      max-width: 75%;
      word-wrap: break-word;
      animation: slideIn 0.3s ease-out;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Typing indicator */
    .typing-indicator {
      display: inline-flex;
      gap: 4px;
      padding: 12px 16px;
      background: #e5e7eb;
      border-radius: 18px;
    }

    .typing-indicator span {
      width: 8px;
      height: 8px;
      background: #9ca3af;
      border-radius: 50%;
      animation: typing 1.4s infinite;
    }

    .typing-indicator span:nth-child(2) {
      animation-delay: 0.2s;
    }

    .typing-indicator span:nth-child(3) {
      animation-delay: 0.4s;
    }

    @keyframes typing {
      0%, 60%, 100% {
        transform: translateY(0);
      }
      30% {
        transform: translateY(-10px);
      }
    }

    /* Badge notification */
    .notification-badge {
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% {
        opacity: 1;
      }
      50% {
        opacity: 0.5;
      }
    }

    /* File preview */
    .file-preview {
      position: relative;
      display: inline-block;
    }

    .file-preview img {
      max-width: 200px;
      max-height: 200px;
      border-radius: 8px;
    }

    .remove-file-btn {
      position: absolute;
      top: -8px;
      right: -8px;
      background: #ef4444;
      color: white;
      border-radius: 50%;
      width: 24px;
      height: 24px;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-size: 14px;
      font-weight: bold;
    }
  </style>
</head>
<body class="h-full bg-gradient-to-br from-slate-50 via-slate-50 to-slate-100 dark:from-slate-950 dark:via-slate-950 dark:to-slate-900 text-slate-800 dark:text-slate-100">
  
  <!-- Audio element for notifications -->
  <audio id="notificationSound" preload="auto">
    <source src="data:audio/wav;base64," type="audio/wav">
  </audio>

  <%
    const convList = Array.isArray(contacts) ? contacts : [];

    function isActive(p){
      return (page === p) ? 'sidebar-link-active' : '';
    }

    function fmtTime(d){
      try {
        const date = new Date(d);
        const now = new Date();
        const diff = now.getTime() - date.getTime();
        const days = Math.floor(diff / (1000 * 60 * 60 * 24));
        
        if (days === 0) {
          return date.toLocaleTimeString('es-PE', { hour: '2-digit', minute: '2-digit' });
        } else if (days === 1) {
          return 'Ayer';
        } else if (days < 7) {
          return date.toLocaleDateString('es-PE', { weekday: 'short' });
        } else {
          return date.toLocaleDateString('es-PE', { day: '2-digit', month: '2-digit' });
        }
      } catch {
        return '';
      }
    }
  %>

  <div class="min-h-screen w-full flex relative">

    <!-- Overlay móvil sidebar -->
    <div id="mobile-sidebar-overlay"
         class="fixed inset-0 bg-black/50 backdrop-blur-sm z-40 opacity-0 pointer-events-none transition-opacity duration-300 md:hidden">
    </div>

    <!-- Sidebar -->
    <aside id="sidebar"
           class="w-72 flex flex-col bg-gradient-to-br from-primary via-primary-600 to-primary-700 text-white shadow-2xl
                  fixed inset-y-0 left-0 z-50 -translate-x-full transition-transform duration-300 ease-out md:translate-x-0 md:relative md:flex"
           role="navigation"
           aria-label="Menú principal">

      <div class="px-6 py-6 border-b border-white/10 bg-white/5 backdrop-blur-sm">
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-xl bg-white/20 backdrop-blur-sm flex items-center justify-center shadow-lg">
              <i data-lucide="bot" class="w-6 h-6"></i>
            </div>
            <div>
              <h2 class="text-lg font-bold">Bot Admin</h2>
              <p class="text-xs text-white/70">Panel de control</p>
            </div>
          </div>

          <button id="close-sidebar-btn"
                  class="md:hidden inline-flex items-center justify-center rounded-lg text-white/70 hover:text-white hover:bg-white/10 p-2 transition"
                  aria-label="Cerrar menú">
            <i data-lucide="x" class="w-5 h-5"></i>
          </button>
        </div>
      </div>

      <nav class="py-4 flex-1 overflow-y-auto">
        <ul class="px-3 space-y-1 text-sm">
          <li>
            <a href="/" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('dashboard') %>">
              <i data-lucide="layout-dashboard" class="w-5 h-5"></i>
              <span>Dashboard</span>
            </a>
          </li>
          <li>
            <a href="/contacts" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('contacts') %>">
              <i data-lucide="users" class="w-5 h-5"></i>
              <span>Contactos</span>
            </a>
          </li>
          <li>
            <a href="/conversations" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('conversations') %>">
              <i data-lucide="message-circle" class="w-5 h-5"></i>
              <span>Conversaciones</span>
            </a>
          </li>
          <li>
            <a href="/blocked" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('blocked') %>">
              <i data-lucide="ban" class="w-5 h-5"></i>
              <span>Bloqueados</span>
            </a>
          </li>
          <li><div class="my-3 h-px bg-white/10"></div></li>
          <li>
            <a href="/calendar" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('calendar') %>">
              <i data-lucide="calendar" class="w-5 h-5"></i>
              <span>Calendario</span>
            </a>
          </li>
          <li>
            <a href="/working-hours" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('working-hours') %>">
              <i data-lucide="clock" class="w-5 h-5"></i>
              <span>Horarios</span>
            </a>
          </li>
          <li><div class="my-3 h-px bg-white/10"></div></li>
          <li>
            <a href="/departments" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('departments') %>">
              <i data-lucide="building-2" class="w-5 h-5"></i>
              <span>Departamentos</span>
            </a>
          </li>
          <li>
            <a href="/products" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('products') %>">
              <i data-lucide="package" class="w-5 h-5"></i>
              <span>Productos</span>
            </a>
          </li>
          <li>
            <a href="/auto-responses" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('auto-responses') %>">
              <i data-lucide="bot" class="w-5 h-5"></i>
              <span>Auto-respuestas</span>
            </a>
          </li>
          <li>
            <a href="/tags" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('tags') %>">
              <i data-lucide="tag" class="w-5 h-5"></i>
              <span>Tags</span>
            </a>
          </li>
          <li>
            <a href="/templates" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('templates') %>">
              <i data-lucide="file-text" class="w-5 h-5"></i>
              <span>Plantillas</span>
            </a>
          </li>
          <li>
            <a href="/metrics" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('metrics') %>">
              <i data-lucide="bar-chart-3" class="w-5 h-5"></i>
              <span>Métricas</span>
            </a>
          </li>
          <li><div class="my-3 h-px bg-white/10"></div></li>
          <li>
            <a href="/admin-users" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('admin-users') %>">
              <i data-lucide="shield" class="w-5 h-5"></i>
              <span>Usuarios Admin</span>
            </a>
          </li>
          <li>
            <a href="/settings" class="group flex items-center gap-3 rounded-xl px-4 py-2.5 text-white/80 hover:text-white hover:bg-white/10 transition-all <%= isActive('settings') %>">
              <i data-lucide="settings" class="w-5 h-5"></i>
              <span>Configuración</span>
            </a>
          </li>
        </ul>
      </nav>

      <div class="mt-auto p-4 bg-black/10 border-t border-white/10 backdrop-blur-sm">
        <div class="rounded-xl bg-white/10 backdrop-blur-sm p-3.5 mb-3">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-full bg-white/20 flex items-center justify-center">
              <i data-lucide="user" class="w-5 h-5"></i>
            </div>
            <div class="flex-1 min-w-0">
              <p class="text-xs text-white/60 font-medium">Sesión activa</p>
              <p class="font-semibold text-sm truncate"><%= user || 'Admin' %></p>
            </div>
          </div>
        </div>
        
        <form method="POST" action="/auth/logout">
          <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <% } %>
          <button type="submit"
                  class="w-full inline-flex items-center justify-center gap-2 rounded-xl bg-red-500/20 hover:bg-red-500/30 backdrop-blur-sm text-white py-2.5 text-sm font-medium transition-all shadow-lg hover:shadow-xl">
            <i data-lucide="log-out" class="w-4 h-4"></i> 
            <span>Cerrar sesión</span>
          </button>
        </form>
      </div>
    </aside>

    <!-- Main Content -->
    <main id="main-content" class="flex-1 p-4 md:p-6 w-full overflow-hidden">

      <!-- Header -->
      <header class="bg-white/70 dark:bg-slate-900/70 backdrop-blur-xl rounded-2xl shadow-card border border-slate-200/50 dark:border-white/5 px-6 py-4 mb-6">
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-3">
            <button id="open-sidebar-btn"
                    class="md:hidden inline-flex items-center justify-center rounded-xl text-slate-600 hover:text-slate-900 dark:text-slate-300 dark:hover:text-white bg-white dark:bg-slate-800 hover:bg-slate-50 dark:hover:bg-slate-700 p-2.5 shadow-soft transition"
                    aria-label="Abrir menú">
              <i data-lucide="menu" class="w-6 h-6"></i>
            </button>
            
            <h1 class="text-2xl font-bold text-slate-900 dark:text-white flex items-center gap-2">
              <i data-lucide="messages-square" class="w-6 h-6 text-primary"></i>
              Conversaciones
            </h1>
          </div>

          <!-- Notification Settings -->
          <div class="flex items-center gap-3">
            <button id="toggleNotifications"
                    class="inline-flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-medium bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 hover:bg-slate-50 dark:hover:bg-slate-700 transition shadow-soft"
                    onclick="toggleNotifications()">
              <i data-lucide="bell" class="w-4 h-4" id="bellIcon"></i>
              <span id="notificationStatus">Sonido ON</span>
            </button>
          </div>
        </div>
      </header>

      <!-- Chat Container -->
      <div class="chat-container">
        
        <!-- Contacts Panel -->
        <div class="contacts-panel bg-white dark:bg-slate-900 rounded-2xl shadow-card border border-slate-200/50 dark:border-white/5 flex flex-col overflow-hidden">
          
          <!-- Search -->
          <div class="p-4 border-b border-slate-200 dark:border-slate-700">
            <div class="relative">
              <i data-lucide="search" class="w-4 h-4 text-slate-400 absolute left-3 top-1/2 -translate-y-1/2"></i>
              <input
                id="searchInput"
                type="text"
                class="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-slate-50 dark:bg-slate-800 pl-10 pr-3 py-2 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/20"
                placeholder="Buscar contacto..."
                onkeyup="filterContacts()"
              />
            </div>
          </div>

          <!-- Contact List -->
          <div id="contactList" class="flex-1 overflow-y-auto">
            <% if (convList.length > 0) { %>
              <% convList.forEach((contact, idx) => { %>
                <div
                  class="contact-item flex items-center gap-3 p-4 border-b border-slate-100 dark:border-slate-800 hover:bg-slate-50 dark:hover:bg-slate-800/50 cursor-pointer transition"
                  data-phone="<%= contact.phoneNumber %>"
                  data-name="<%= contact.name || contact.phoneNumber %>"
                  onclick="openChat('<%= contact.phoneNumber %>', '<%= contact.name || contact.phoneNumber %>')"
                >
                  <!-- Avatar -->
                  <div class="w-12 h-12 rounded-full bg-gradient-to-br from-primary to-primary-700 flex items-center justify-center text-white font-semibold flex-shrink-0">
                    <%= (contact.name || contact.phoneNumber).substring(0, 2).toUpperCase() %>
                  </div>

                  <!-- Info -->
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center justify-between mb-1">
                      <h3 class="font-semibold text-slate-900 dark:text-white truncate">
                        <%= contact.name || contact.phoneNumber %>
                      </h3>
                      <span class="text-xs text-slate-500">
                        <%= fmtTime(contact.updatedAt) %>
                      </span>
                    </div>
                    <p class="text-sm text-slate-500 dark:text-slate-400 truncate">
                      <%= contact.phoneNumber %>
                    </p>
                  </div>

                  <!-- Unread badge (placeholder) -->
                  <div class="unread-badge hidden w-6 h-6 rounded-full bg-whatsapp text-white text-xs font-bold flex items-center justify-center notification-badge">
                    0
                  </div>
                </div>
              <% }) %>
            <% } else { %>
              <div class="flex flex-col items-center justify-center h-full p-8 text-center">
                <i data-lucide="inbox" class="w-16 h-16 text-slate-300 dark:text-slate-700 mb-4"></i>
                <p class="text-slate-500 dark:text-slate-400 font-medium">No hay conversaciones</p>
                <p class="text-sm text-slate-400 dark:text-slate-500 mt-1">Las conversaciones aparecerán aquí</p>
              </div>
            <% } %>
          </div>
        </div>

        <!-- Chat Panel -->
        <div id="chatPanel" class="bg-white dark:bg-slate-900 rounded-2xl shadow-card border border-slate-200/50 dark:border-white/5 flex flex-col overflow-hidden">
          
          <!-- Empty State -->
          <div id="emptyState" class="flex flex-col items-center justify-center h-full p-8 text-center">
            <div class="w-24 h-24 rounded-full bg-gradient-to-br from-primary/10 to-primary/5 flex items-center justify-center mb-6">
              <i data-lucide="message-circle" class="w-12 h-12 text-primary"></i>
            </div>
            <h2 class="text-2xl font-bold text-slate-800 dark:text-white mb-2">Selecciona una conversación</h2>
            <p class="text-slate-500 dark:text-slate-400">Elige un contacto para ver el historial y enviar mensajes</p>
          </div>

          <!-- Active Chat (hidden by default) -->
          <div id="activeChat" class="hidden h-full flex flex-col">
            
            <!-- Chat Header -->
            <div class="flex items-center justify-between px-6 py-4 border-b border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800/50">
              <div class="flex items-center gap-3">
                <button class="md:hidden" onclick="closeChat()">
                  <i data-lucide="arrow-left" class="w-5 h-5"></i>
                </button>
                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-primary to-primary-700 flex items-center justify-center text-white font-semibold">
                  <span id="chatAvatar">A</span>
                </div>
                <div>
                  <h3 id="chatName" class="font-bold text-slate-900 dark:text-white">Contacto</h3>
                  <p id="chatPhone" class="text-sm text-slate-500 dark:text-slate-400">+51 999 999 999</p>
                </div>
              </div>

              <div class="flex items-center gap-2">
                <button
                  class="inline-flex items-center gap-2 rounded-lg px-3 py-2 text-sm font-medium bg-rose-500 hover:bg-rose-600 text-white transition shadow-soft"
                  onclick="deleteCurrentConversation()">
                  <i data-lucide="trash-2" class="w-4 h-4"></i>
                  <span class="hidden sm:inline">Eliminar</span>
                </button>
              </div>
            </div>

            <!-- Messages Container -->
            <div id="messagesContainer" class="flex-1 overflow-y-auto p-6 space-y-3 bg-slate-50 dark:bg-slate-900 chat-messages">
              <!-- Messages will be loaded here -->
            </div>

            <!-- Typing Indicator -->
            <div id="typingIndicator" class="hidden px-6 py-2">
              <div class="typing-indicator">
                <span></span>
                <span></span>
                <span></span>
              </div>
            </div>

            <!-- Message Input -->
            <div class="border-t border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 p-4">
              
              <!-- File Preview -->
              <div id="filePreviewContainer" class="hidden mb-3 p-3 bg-slate-50 dark:bg-slate-900 rounded-lg">
                <div class="flex items-start gap-3">
                  <div id="filePreview" class="file-preview"></div>
                  <div class="flex-1">
                    <p id="fileName" class="text-sm font-medium text-slate-700 dark:text-slate-300"></p>
                    <p id="fileSize" class="text-xs text-slate-500"></p>
                  </div>
                  <button onclick="removeFile()" class="text-rose-500 hover:text-rose-600">
                    <i data-lucide="x" class="w-5 h-5"></i>
                  </button>
                </div>
              </div>

              <form id="messageForm" class="flex items-end gap-2">
                <input type="file" id="fileInput" class="hidden" accept="image/*,video/*,audio/*,.pdf,.doc,.docx,.xls,.xlsx,.zip" onchange="handleFileSelect(event)">
                
                <button type="button"
                        class="flex-shrink-0 inline-flex items-center justify-center w-10 h-10 rounded-lg bg-slate-100 dark:bg-slate-700 hover:bg-slate-200 dark:hover:bg-slate-600 text-slate-600 dark:text-slate-300 transition"
                        onclick="document.getElementById('fileInput').click()">
                  <i data-lucide="paperclip" class="w-5 h-5"></i>
                </button>

                <div class="flex-1 relative">
                  <textarea
                    id="messageInput"
                    rows="1"
                    class="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-900 px-4 py-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/20 resize-none"
                    placeholder="Escribe un mensaje..."
                    onkeydown="handleKeyDown(event)"
                    oninput="autoResize(this)"
                  ></textarea>
                </div>

                <button type="submit"
                        class="flex-shrink-0 inline-flex items-center justify-center w-10 h-10 rounded-lg bg-whatsapp hover:bg-whatsapp-dark text-white transition shadow-soft">
                  <i data-lucide="send" class="w-5 h-5"></i>
                </button>
              </form>

              <div id="sendStatus" class="hidden mt-2 text-xs text-center"></div>
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>

  <script>
    // Global state
    let currentPhone = null;
    let currentName = null;
    let notificationsEnabled = localStorage.getItem('notificationsEnabled') !== 'false';
    let lastMessageCheck = new Date().toISOString();
    let selectedFile = null;
    let isTyping = false;
    let typingTimeout = null;

    // Initialize
    window.lucide && lucide.createIcons();
    updateNotificationButton();
    startMessagePolling();

    // Sidebar mobile
    (function(){
      const sidebar = document.getElementById('sidebar');
      const overlay = document.getElementById('mobile-sidebar-overlay');
      const openBtn = document.getElementById('open-sidebar-btn');
      const closeBtn = document.getElementById('close-sidebar-btn');

      function openSidebar(){
        sidebar.classList.remove('-translate-x-full');
        overlay.classList.remove('opacity-0','pointer-events-none');
      }
      function closeSidebar(){
        sidebar.classList.add('-translate-x-full');
        overlay.classList.add('opacity-0','pointer-events-none');
      }

      if (openBtn) openBtn.addEventListener('click', openSidebar);
      if (closeBtn) closeBtn.addEventListener('click', closeSidebar);
      if (overlay) overlay.addEventListener('click', closeSidebar);
    })();

    // Notification toggle
    function toggleNotifications() {
      notificationsEnabled = !notificationsEnabled;
      localStorage.setItem('notificationsEnabled', notificationsEnabled);
      updateNotificationButton();
      
      if (notificationsEnabled) {
        showToast('✅ Notificaciones activadas', 'success');
        playNotificationSound(); // Test sound
      } else {
        showToast('🔕 Notificaciones desactivadas', 'info');
      }
    }

    function updateNotificationButton() {
      const btn = document.getElementById('toggleNotifications');
      const icon = document.getElementById('bellIcon');
      const status = document.getElementById('notificationStatus');
      
      if (notificationsEnabled) {
        btn.classList.remove('opacity-50');
        icon.setAttribute('data-lucide', 'bell');
        status.textContent = 'Sonido ON';
      } else {
        btn.classList.add('opacity-50');
        icon.setAttribute('data-lucide', 'bell-off');
        status.textContent = 'Sonido OFF';
      }
      
      window.lucide && lucide.createIcons();
    }

    // Play notification sound
    function playNotificationSound() {
      if (!notificationsEnabled) return;
      
      const audio = document.getElementById('notificationSound');
      audio.play().catch(err => console.log('Could not play sound:', err));
    }

    // Filter contacts
    function filterContacts() {
      const input = document.getElementById('searchInput');
      const filter = input.value.toLowerCase();
      const contacts = document.querySelectorAll('.contact-item');

      contacts.forEach(contact => {
        const phone = contact.dataset.phone.toLowerCase();
        const name = contact.dataset.name.toLowerCase();
        
        if (phone.includes(filter) || name.includes(filter)) {
          contact.style.display = '';
        } else {
          contact.style.display = 'none';
        }
      });
    }

    // Open chat
    async function openChat(phone, name) {
      currentPhone = phone;
      currentName = name;

      // Update UI
      document.getElementById('emptyState').classList.add('hidden');
      document.getElementById('activeChat').classList.remove('hidden');
      document.getElementById('chatName').textContent = name;
      document.getElementById('chatPhone').textContent = phone;
      document.getElementById('chatAvatar').textContent = name.substring(0, 2).toUpperCase();

      // Mark as active
      document.querySelectorAll('.contact-item').forEach(item => {
        item.classList.remove('bg-primary/10');
      });
      document.querySelector(`[data-phone="${phone}"]`)?.classList.add('bg-primary/10');

      // Hide on mobile
      if (window.innerWidth < 768) {
        document.querySelector('.contacts-panel').classList.add('hidden-mobile');
      }

      // Load messages
      await loadMessages(phone);

      // Clear unread badge
      const badge = document.querySelector(`[data-phone="${phone}"] .unread-badge`);
      if (badge) {
        badge.classList.add('hidden');
        badge.textContent = '0';
      }
    }

    // Close chat (mobile)
    function closeChat() {
      document.getElementById('emptyState').classList.remove('hidden');
      document.getElementById('activeChat').classList.add('hidden');
      document.querySelector('.contacts-panel').classList.remove('hidden-mobile');
      currentPhone = null;
      currentName = null;
    }

    // Load messages
    async function loadMessages(phone) {
      try {
        const res = await fetch(`/conversations/api/${phone}`);
        const data = await res.json();

        const container = document.getElementById('messagesContainer');
        
        if (data.messages && data.messages.length > 0) {
          container.innerHTML = data.messages.map(msg => renderMessage(msg)).join('');
        } else {
          container.innerHTML = `
            <div class="flex flex-col items-center justify-center h-full text-center text-slate-400">
              <i data-lucide="inbox" class="w-12 h-12 mb-3 opacity-50"></i>
              <p class="text-sm">No hay mensajes en esta conversación</p>
            </div>
          `;
        }

        window.lucide && lucide.createIcons();
        scrollToBottom();
      } catch (error) {
        console.error('Error loading messages:', error);
        showToast('❌ Error al cargar mensajes', 'error');
      }
    }

    // Render message
    function renderMessage(msg) {
      const isUser = msg.role === 'USER';
      const time = new Date(msg.createdAt).toLocaleTimeString('es-PE', { hour: '2-digit', minute: '2-digit' });

      return `
        <div class="flex ${isUser ? 'justify-end' : 'justify-start'}">
          <div class="message-bubble ${isUser ? 'bg-primary text-white rounded-br-none' : 'bg-white dark:bg-slate-800 text-slate-900 dark:text-white rounded-bl-none'} px-4 py-2 rounded-2xl shadow-soft">
            <p class="text-sm whitespace-pre-wrap break-words">${escapeHtml(msg.content)}</p>
            <p class="text-xs ${isUser ? 'text-white/70' : 'text-slate-400'} mt-1 text-right">${time}</p>
          </div>
        </div>
      `;
    }

    // File handling
    function handleFileSelect(event) {
      const file = event.target.files[0];
      if (!file) return;

      selectedFile = file;

      // Show preview
      const container = document.getElementById('filePreviewContainer');
      const preview = document.getElementById('filePreview');
      const fileName = document.getElementById('fileName');
      const fileSize = document.getElementById('fileSize');

      fileName.textContent = file.name;
      fileSize.textContent = formatFileSize(file.size);

      // Image preview
      if (file.type.startsWith('image/')) {
        const reader = new FileReader();
        reader.onload = (e) => {
          preview.innerHTML = `<img src="${e.target.result}" alt="Preview" class="max-w-[150px] max-h-[150px] rounded-lg">`;
        };
        reader.readAsDataURL(file);
      } else {
        preview.innerHTML = `<div class="w-16 h-16 rounded-lg bg-slate-100 dark:bg-slate-800 flex items-center justify-center"><i data-lucide="file" class="w-8 h-8 text-slate-400"></i></div>`;
        window.lucide && lucide.createIcons();
      }

      container.classList.remove('hidden');
    }

    function removeFile() {
      selectedFile = null;
      document.getElementById('fileInput').value = '';
      document.getElementById('filePreviewContainer').classList.add('hidden');
    }

    function formatFileSize(bytes) {
      if (bytes < 1024) return bytes + ' B';
      if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(1) + ' KB';
      return (bytes / (1024 * 1024)).toFixed(1) + ' MB';
    }

    // Send message
    document.getElementById('messageForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      if (!currentPhone) {
        showToast('⚠️ Selecciona un contacto primero', 'warning');
        return;
      }

      const input = document.getElementById('messageInput');
      const message = input.value.trim();

      // Send file if selected
      if (selectedFile) {
        await sendFile();
        return;
      }

      // Send text message
      if (!message) return;

      try {
        showSendStatus('Enviando...', 'loading');

        const res = await fetch(`/conversations/api/${currentPhone}/send`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ message })
        });

        const data = await res.json();

        if (data.success) {
          input.value = '';
          input.style.height = 'auto';
          
          // Add message to UI
          const container = document.getElementById('messagesContainer');
          const emptyMsg = container.querySelector('.flex.flex-col.items-center');
          if (emptyMsg) container.innerHTML = '';

          container.innerHTML += renderMessage({
            role: 'ASSISTANT',
            content: message,
            createdAt: new Date().toISOString()
          });

          window.lucide && lucide.createIcons();
          scrollToBottom();
          showSendStatus('✓ Enviado', 'success');
          
          setTimeout(() => hideSendStatus(), 2000);
        } else {
          showSendStatus('✗ Error: ' + data.error, 'error');
        }
      } catch (error) {
        console.error('Error sending message:', error);
        showSendStatus('✗ Error al enviar', 'error');
      }
    });

    // Send file
    async function sendFile() {
      if (!selectedFile || !currentPhone) return;

      try {
        showSendStatus('Enviando archivo...', 'loading');

        const formData = new FormData();
        formData.append('file', selectedFile);
        formData.append('caption', document.getElementById('messageInput').value.trim());

        const res = await fetch(`/conversations/api/${currentPhone}/send-media`, {
          method: 'POST',
          body: formData
        });

        const data = await res.json();

        if (data.success) {
          document.getElementById('messageInput').value = '';
          removeFile();

          // Add to UI
          const container = document.getElementById('messagesContainer');
          container.innerHTML += renderMessage({
            role: 'ASSISTANT',
            content: `📎 ${selectedFile.name}`,
            createdAt: new Date().toISOString()
          });

          window.lucide && lucide.createIcons();
          scrollToBottom();
          showSendStatus('✓ Archivo enviado', 'success');
          
          setTimeout(() => hideSendStatus(), 2000);
        } else {
          showSendStatus('✗ Error: ' + data.error, 'error');
        }
      } catch (error) {
        console.error('Error sending file:', error);
        showSendStatus('✗ Error al enviar archivo', 'error');
      }
    }

    // Send status
    function showSendStatus(text, type) {
      const status = document.getElementById('sendStatus');
      status.textContent = text;
      status.className = `mt-2 text-xs text-center ${
        type === 'loading' ? 'text-blue-600' :
        type === 'success' ? 'text-green-600' :
        type === 'error' ? 'text-red-600' : ''
      }`;
      status.classList.remove('hidden');
    }

    function hideSendStatus() {
      document.getElementById('sendStatus').classList.add('hidden');
    }

    // Auto-resize textarea
    function autoResize(textarea) {
      textarea.style.height = 'auto';
      textarea.style.height = Math.min(textarea.scrollHeight, 150) + 'px';
    }

    // Handle Enter key
    function handleKeyDown(event) {
      if (event.key === 'Enter' && !event.shiftKey) {
        event.preventDefault();
        document.getElementById('messageForm').dispatchEvent(new Event('submit'));
      }
    }

    // Delete conversation
    async function deleteCurrentConversation() {
      if (!currentPhone) return;
      
      if (!confirm(`¿Eliminar toda la conversación con ${currentName}?\n\nEsta acción no se puede deshacer.`)) return;

      try {
        const res = await fetch(`/conversations/api/${currentPhone}`, {
          method: 'DELETE'
        });

        const data = await res.json();

        if (data.success) {
          showToast('✅ Conversación eliminada', 'success');
          closeChat();
          
          // Remove from list
          document.querySelector(`[data-phone="${currentPhone}"]`)?.remove();
        } else {
          showToast('❌ Error al eliminar', 'error');
        }
      } catch (error) {
        console.error('Error deleting conversation:', error);
        showToast('❌ Error al eliminar', 'error');
      }
    }

    // Message polling
    function startMessagePolling() {
      setInterval(checkNewMessages, 3000); // Check every 3 seconds
    }

    async function checkNewMessages() {
      try {
        const res = await fetch(`/conversations/api/new-messages/check?lastCheck=${lastMessageCheck}`);
        const data = await res.json();

        if (data.hasNew && data.messages.length > 0) {
          lastMessageCheck = new Date().toISOString();

          // Play sound for new messages
          playNotificationSound();

          // Update badges
          data.messages.forEach(msg => {
            const badge = document.querySelector(`[data-phone="${msg.phoneNumber}"] .unread-badge`);
            if (badge) {
              const current = parseInt(badge.textContent) || 0;
              badge.textContent = current + 1;
              badge.classList.remove('hidden');
            }
          });

          // If chat is open, reload messages
          if (currentPhone && data.messages.some(m => m.phoneNumber === currentPhone)) {
            await loadMessages(currentPhone);
          }

          showToast(`💬 ${data.count} mensaje(s) nuevo(s)`, 'info');
        }
      } catch (error) {
        console.error('Error checking messages:', error);
      }
    }

    // Toast notification
    function showToast(message, type = 'info') {
      const toast = document.createElement('div');
      toast.className = `fixed bottom-6 right-6 z-50 px-6 py-3 rounded-lg shadow-lg text-white font-medium transform translate-y-0 transition-all duration-300 ${
        type === 'success' ? 'bg-green-500' :
        type === 'error' ? 'bg-red-500' :
        type === 'warning' ? 'bg-amber-500' :
        'bg-blue-500'
      }`;
      toast.textContent = message;
      
      document.body.appendChild(toast);
      
      setTimeout(() => {
        toast.style.transform = 'translateY(100px)';
        toast.style.opacity = '0';
        setTimeout(() => toast.remove(), 300);
      }, 3000);
    }

    // Scroll to bottom
    function scrollToBottom() {
      const container = document.getElementById('messagesContainer');
      requestAnimationFrame(() => {
        container.scrollTop = container.scrollHeight;
      });
    }

    // Escape HTML
    function escapeHtml(text) {
      const div = document.createElement('div');
      div.textContent = text;
      return div.innerHTML;
    }
  </script>
</body>
</html>